mcdp {
    provides revenue [$]

    requires xsubsidy [$]
    requires purchase_cost [$]
    requires fixed_cost_frac [dimensionless]
    requires fixed_costs [$]

    required xsubsidy + required purchase_cost + required fixed_cost_frac * required fixed_costs >= provided revenue
}
