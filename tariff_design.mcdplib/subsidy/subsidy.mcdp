mcdp {
    provides revenue [$]

    required xsubsidy [$]
    required purchase_cost [$]
    required fixed_cost_frac [dimensionless]
    required fixed_costs [$]

    required xsubsidy + required purchase_cost + required fixed_cost_frac * required fixed_costs <= provided revenue
}
