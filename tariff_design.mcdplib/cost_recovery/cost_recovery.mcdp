mcdp {
    provides recovery_rate [dimensionless]
    provides market_buy_cost [$]
    provides fixed_costs [$]
    requires revenue_A [$]
    requires revenue_B [$]

    provided recovery_rate * (provided market_buy_cost + provided fixed_costs) <= (required revenue_A + required revenue_B)
}